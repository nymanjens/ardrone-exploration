exec camera.cfg

// ********* custom settings (reslab) ********* //
Custom.SURFInitialization=1
Custom.FASTMapExpansion=1
Custom.FASTMapExpansion2=1
Custom.SecondCamera=0

// ********* PTAMM_VTOL settings ********* //
MapMaker.MaxKFDistWiggleMult = 0.5 // Used to determine min stereo separation:
    // requiredDistance = MaxKFDistWiggleMult * mdWiggleScaleDepthNormalized * kCurrent.dSceneDepthMean
    // where mdWiggleScaleDepthNormalized = mdWiggleScale / pkFirst->dSceneDepthMean;
    // default is 1.0
MapMaker.MinKFsVectorDiffInDeg = 13.0 // = requiredAngle (default = 25.0)
